bs4DashPage(
  title = tit_app,  dark = NULL, scrollToTop = TRUE, help = NULL,
  preloader = preloader_inicio,
  header = header,
  sidebar = sidebar,
  body = body,
  controlbar = controlbar,
  footer = footer
)
